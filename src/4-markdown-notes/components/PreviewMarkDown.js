import styled from 'styled-components'

const PreviewStyled = styled.div`
  background-color: aqua;
`
export const PreviewMarkDown = () => (
  <PreviewStyled className='item item3'>this is the preview</PreviewStyled>
)
